  <header class="p-3 bg-dark text-white">
    <div class="container">
      <div class="d-flex flex-wrap align-items-center justify-content-center justify-content-lg-start">
        <a href="/" class="d-flex align-items-center mb-2 mb-lg-0 text-white text-decoration-none">
              <!-- Logo -->
        </a>
        <ul class="nav col-12 col-lg-auto me-lg-auto mb-2 justify-content-center mb-md-0">
          <li><a href="/upcommingsneakers" class="nav-link px-2 text-secondary">DROPS À VENIR</a></li>
          <li><a href="/latestsneakers" class="nav-link px-2 text-white">DROPS PASSÉS</a></li>
        </ul>
        <p>
          <% if user_signed_in? %>
            Logged in as <strong><%= current_user.email %></strong>.
            <%= link_to 'Profil', edit_user_registration_path, :class => 'navbar-link' %> |
            <%= link_to "Deconnexion", destroy_user_session_path, method: :delete, :class => 'navbar-link'  %>
          <% else %>
            <%= link_to "Inscription", new_user_registration_path, :class => 'navbar-link'  %> |
            <%= link_to "Connexion", new_user_session_path, :class => 'navbar-link'  %>
          <% end %>
        </p>
      </div>
    </div>
  </header>
